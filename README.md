# Retail Monitoring — автоматизация контроля торговых точек

**Заказчик:** Сеть супермаркетов, 150+ торговых точек  
**Задача:** Еженедельно выявлять точки с аномально низким средним чеком, отсекая те, где недостаточно данных для статистической значимости

---

## Структура проекта

### 📁 Модуль 1. `store_control.ipynb`

- Расчёт среднего чека по сети и по каждой точке
- Функция проверки с порогами:
  - `low_check` — средний чек ниже 90% от сетевого
  - `low_data` — мало транзакций (<300), доверия к среднему нет
  - `ok` — точка в норме
- Применение ко всем точкам, расчёт доли «зелёных»
- Выгрузка списка точек для контроля

### 📁 Модуль 2. `data_aggregator.ipynb`

Универсальный агрегатор для «грязных» выгрузок.

**Проблема:** в регулярных отчётах CRM поля содержат числа, списки чисел, строки, пропуски.  
**Решение:** единая функция `aggregate_values()`, которая:

- обрабатывает словари и списки любой вложенности
- игнорирует строки, None, булевы значения
- учитывает порог отсечения (только числа >= threshold)
- возвращает сумму, среднее, количество чисел

Функция готова к встраиванию в ETL-процесс и протестирована на разных сценариях.

---

## Стек

- Python (pandas, базовые структуры)
- Jupyter Notebook
- Формат данных: Excel, CSV

---

## Результаты для бизнеса

- **Автоматизация ручного контроля** — вместо 2 часов работы аналитика еженедельно
- **Прозрачный критерий** «проблемности» точки
- **Возможность масштабирования** на новые регионы
